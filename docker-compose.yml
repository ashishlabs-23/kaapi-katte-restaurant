version: '3.8'

services:
  kaapi-katte-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kaapi_katte_production
    ports:
      - "8080:80"
    restart: always
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
